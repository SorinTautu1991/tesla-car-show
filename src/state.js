import * as THREE from "three";

const state = {
  activeMesh: {},
  activeMeshName: "body_primary_0",
  cameraPos: new THREE.Vector3(-4, 7, -10),
  target: new THREE.Vector3(1, 2, -8),
  shouldUpdate: false,
};

export default state;
